// Optionally, you can add interactivity using JavaScript
document.querySelectorAll('.join').forEach(button => {
    button.addEventListener('click', () => {
        alert('You have joined this job!');
    });
});